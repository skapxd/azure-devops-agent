{
  // Las reglas de @skapxd/lint-agent se ven mientras se escribe, no solo al
  // ejecutar `pnpm lint`.
  "eslint.useFlatConfig": true,
  "eslint.validate": ["typescript", "javascript"],
  "eslint.workingDirectories": [{ "mode": "auto" }],

  // El proyecto usa la versión de TypeScript instalada, no la que trae VS Code,
  // para que editor y CLI reporten exactamente lo mismo.
  "typescript.tsdk": "node_modules/typescript/lib",
  "typescript.enablePromptUseWorkspaceTsdk": true,

  // El explorador de pruebas usa el vitest del proyecto, no uno global, para que
  // el editor corra exactamente lo mismo que `pnpm test`.
  "vitest.nodeExecutable": "node",
  "vitest.rootConfig": "./vitest.config.ts",

  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  }
}
